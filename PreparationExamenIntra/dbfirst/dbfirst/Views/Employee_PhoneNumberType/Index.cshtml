@model IEnumerable<dbfirst.Models.Employee_PhoneNumberType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNumbertype)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumbertype)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhoneNumberTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhoneNumberTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhoneNumberTypeID })
        </td>
    </tr>
}

</table>
